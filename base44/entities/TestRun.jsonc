{
  "name": "TestRun",
  "type": "object",
  "properties": {
    "label": {
      "type": "string"
    },
    "site_key": {
      "type": "string"
    },
    "target_site_keys": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional override of testing targets (e.g. ['joe'] to test Double creds against Joe only)."
    },
    "custom_url": {
      "type": "string",
      "description": "Optional custom login URL that overrides the site's login_url for this run."
    },
    "status": {
      "type": "string",
      "enum": [
        "queued",
        "running",
        "completed",
        "cancelled",
        "failed"
      ],
      "default": "queued"
    },
    "concurrency": {
      "type": "number",
      "default": 2
    },
    "max_retries": {
      "type": "number",
      "default": 1
    },
    "login_strategy": {
      "type": "string",
      "enum": [
        "single",
        "multi_password",
        "all_passwords"
      ],
      "description": "Per-run login strategy. Falls back to AppSettings."
    },
    "proxy_mode": {
      "type": "string",
      "enum": [
        "classic",
        "premium",
        "stealth",
        "external",
        "pool",
        "none"
      ],
      "description": "ScrapingBee proxy tier override. Falls back to AppSettings."
    },
    "country_code": {
      "type": "string",
      "description": "ISO-2 country code (only honored on premium / stealth tiers)."
    },
    "external_proxy_id": {
      "type": "string"
    },
    "proxy_pool_id": {
      "type": "string"
    },
    "total_count": {
      "type": "number",
      "default": 0
    },
    "pending_count": {
      "type": "number",
      "default": 0
    },
    "working_count": {
      "type": "number",
      "default": 0
    },
    "failed_count": {
      "type": "number",
      "default": 0
    },
    "error_count": {
      "type": "number",
      "default": 0
    },
    "started_at": {
      "type": "string",
      "format": "date-time"
    },
    "ended_at": {
      "type": "string",
      "format": "date-time"
    },
    "elapsed_ms": {
      "type": "number",
      "default": 0
    }
  },
  "required": [
    "site_key"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}