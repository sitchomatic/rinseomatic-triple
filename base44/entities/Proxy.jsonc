{
  "name": "Proxy",
  "type": "object",
  "properties": {
    "label": {
      "type": "string"
    },
    "host": {
      "type": "string"
    },
    "port": {
      "type": "number"
    },
    "protocol": {
      "type": "string",
      "enum": [
        "http",
        "https",
        "socks5",
        "wireguard"
      ],
      "default": "http"
    },
    "username": {
      "type": "string",
      "description": "Optional proxy auth username"
    },
    "password": {
      "type": "string",
      "description": "Optional proxy auth password"
    },
    "wireguard_config": {
      "type": "string",
      "description": "Full NordLynx / WireGuard .conf file contents (PrivateKey, PublicKey, Endpoint, AllowedIPs). Used only when protocol = 'wireguard'."
    },
    "region": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "healthy",
        "degraded",
        "down",
        "untested"
      ],
      "default": "untested"
    },
    "latency_ms": {
      "type": "number"
    },
    "last_check": {
      "type": "string",
      "format": "date-time"
    },
    "last_ip": {
      "type": "string",
      "description": "Last public IP observed when pinging this proxy."
    },
    "last_country": {
      "type": "string",
      "description": "Last country code observed when pinging this proxy."
    },
    "rotation_weight": {
      "type": "number",
      "default": 1
    },
    "enabled": {
      "type": "boolean",
      "default": true
    },
    "total_pings": {
      "type": "number",
      "default": 0
    },
    "failed_pings": {
      "type": "number",
      "default": 0
    },
    "consecutive_failures": {
      "type": "number",
      "default": 0
    }
  },
  "required": [
    "host"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {},
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}